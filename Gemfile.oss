# This Gemfile extends the base Gemfile with OSS-specific dependencies
eval_gemfile "Gemfile"

git_source(:oss) { |repo| "https://github.com/#{ENV['GH_USER']}/#{repo}" }

gem "fizzy-oss", oss: "fizzy-oss"